/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/mon-portfolio',
  assetPrefix: '/mon-portfolio/',
  trailingSlash: true,
};

module.exports = nextConfig;
